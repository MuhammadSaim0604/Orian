# Consumer ProGuard rules for the bridge module.
#
# The React Native module reflects over this module's classes only through the
# Turbo Module interface, so nothing extra needs keeping here yet. Rules are added
# if release builds start stripping something the bridge depends on.
