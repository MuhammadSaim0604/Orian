<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      The accessibility service is declared in the library that owns it, so any
      app module consuming this library gets a correct declaration by merge
      rather than having to restate it.

      BIND_ACCESSIBILITY_SERVICE is a signature-level permission held by the
      system: it guarantees only the OS can bind this service.
    -->
    <application>
        <service
            android:name=".service.UiAutomationAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>
</manifest>
