{
  "name": "@mobile-automation/prompt-engine",
  "version": "0.1.0",
  "description": "Prompt templates, model context assembly, and structured output parsing",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --emitDeclarationOnly",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@mobile-automation/shared-types": "workspace:*",
    "@mobile-automation/tool-sdk": "workspace:*",
    "zod": "3.24.1"
  }
}
