
A automation workflow execution like n8n.
But this is for automating mobile means like automatically reading screen, performing actions like click, swipe, long press, or etc... using accessibility, device default assistant and every other type of permissions that require for automating the mobile phone. Remember that There will be a proper mobile screen based canvas, smooth and professional, and proper nodes and proper logic nodes like in n8n, properly creating nodes classes and importing feature to import any type of custom nodes like in n8n means a separate nodes sdk which will be use by our application and also other developers can install it from npm by using npm install. A proper monorepo project, proper eslint and build scripts, and remember that proper unit testing means a fully professional MonoRepo like.


Main Functionality, An Ai Agent and a workflow Execution Engine, and a workflow builder, and a Nodes package.


Execution:

We can create workflows by asking the ai agent that will automatically chose the nodes and configure them and properly read the screen go and execute the whole workflow first then properly remember and saves the click cordinates in every step, properly saves the information to where click , which android element tree to click, how to filter, how to add logics, means it will save whole results after executing the workflow by ai. Then it will start building the automation workflow by using this results data so that it knows in which screen in which step how to filter and detect the buttons, where to do what and etc.. then accordingly create the workflow.

And also remember that there is another feature in manually node creation not with ai which is default workflow creation choice, in the manually creation when we create a node something like if condition and I want to ask that if this button is visible on the screen or in the UI then pass true other wise false now I can set this node bt using ai means a proper full floating menu floats on the screen when I click on configure with ai on this particular node then a floating menu will open not showing all options in the menu because it will cover the most of the screen show few options in the floating menu and a button of more or eye button which unhides the all other buttons and features over the screen. Now we clicked on configure with AI button on this particular node now floating menu shows and I will go to that particular screen step where I want to add this IF logic here I will ask the agent to analyse this screen to pass true if this particular button is showing otherwise pass false. We can write our prompt in the floating menu. Means in short floating menu is the whole toolset where I can analyze the screen see the screen element tree or find cordinates or do anything. Only a button of ai agent where I can ask the ai agent to analyze this screen and configure it in a way, and remember that floating menu shows with node I'd means if we are on the if logic node in the canvas in the app then we open floating menu toolset on behalf of this node so that the ai agent inside the floating toolset automatically understands I am configuring which node and how to configure.

Make sure proper prompt creation engine for everything.


Also make sure that there will be a one more different automation option, means our app has two layers of automation, one is ai agent and one is workflow based. In Ai Agent we will ask it like "Send message to Robert on WhatsApp that I will late tomorrow" then Ai agent will properly plans fastly, open the app by using Android API, search for Robert or etc.m or  scroll by chats then it see Robert chat and clicked on it and fastly typed the message and sent it. This agent has its own infrastructure of Ai Agent looping, it can read screen as screenshot and Android UI tree, Tool set of every Android API from screen to contacts to alarms to schedules to everything a proper tool set. Also it exposes these tools with MCP server so that we can connect with any other ai agent. Remember currently only supports Chat completions providers only.


Another layer is workflow based.

Here inside this workflow based system also we have two more options of create by ai or create manually and also option of create with ai agent inside the workflow builder.

Now I just gave you every aspect that is in my mind.
