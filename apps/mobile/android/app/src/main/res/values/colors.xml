<resources>
    <!-- Mirrors the `background` semantic token, light scheme. -->
    <color name="app_background">#f8fafc</color>

    <!-- Launcher icon background: the `primary` brand token. -->
    <color name="ic_launcher_background">#4f46e5</color>
</resources>
